# Cloudflare Pages redirect rules

# Static admin panel (must come BEFORE the SPA catch-all)
/admin     /admin.html    200
/privacy   /privacy.html  200

# SPA fallback — send all unmatched routes to index.html for React Router
/*    /index.html    200
